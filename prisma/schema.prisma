// Prisma schema for QuickNote
// Switch datasource url to postgresql:// for production

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// TODO: define User, Account (NextAuth), Note, NudgeSchedule models
