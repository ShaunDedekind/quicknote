// POST /api/notes — ingest a new note (text or transcribed audio)
// Validate with Zod, persist raw note, trigger expansion
// TODO: implement handler

export function POST() {
  return new Response('Not yet implemented', { status: 501 });
}
