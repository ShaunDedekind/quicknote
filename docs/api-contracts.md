# API Contracts

> Reference doc — load with `@docs/api-contracts.md`

## TODO: document internal API shape contracts
- POST /api/notes — request & response shapes
- POST /api/notes/expand — request & response shapes
- POST /api/integrations/calendar — request & response shapes
